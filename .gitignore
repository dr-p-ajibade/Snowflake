# Snowflake CLI / connector credentials — never commit these
config.toml
connections.toml
*.pem
*.p8
*.p8.pub
.env

# Local chart output (regenerate with analysis/dashboard.py; keep the repo
# small — copy the specific PNGs you want to showcase into README assets)
analysis/output/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS cruft
.DS_Store
